service: my-backend-service
provider:
  name: aws
  runtime: nodejs16.x
  region: ap-south-1
functions:
  api:
    handler: handler.api
    events:
      - http:
          path: /
          method: get
